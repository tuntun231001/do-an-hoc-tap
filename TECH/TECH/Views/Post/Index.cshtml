@using TECH.Areas.Admin.Models
@using TECH.Utilities
@model PagedResult<PostModelView>
@{
    ViewData["Title"] = "Home Page";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
    <style>
    .blog-entry .text .heading {
        font-size: 20px;
        margin-bottom: 16px;
        text-overflow: ellipsis;
        -webkit-line-clamp: 2;
        overflow: hidden;
        display: -webkit-box;
        /* margin: 10px 0px; */
        margin-top: 0px;
        margin-bottom: 5px;
        font-weight: bold;
        color: #0098da;
    }

    .short-content {
        text-overflow: ellipsis;
        /*-webkit-line-clamp: 3;*/
        -webkit-box-orient: vertical;
        -webkit-line-clamp: 5;
        overflow: hidden;
        display: -webkit-box;
        margin-top: 0px;
        font-size: 15px;
        color: #333e48;
        font-weight: bold;
    }

    .btn-search-post {
        height: 95%;
        width: 46px;
        margin-top: 1px;
        margin-right: 0px;
        display: flex;
        justify-content: center;
        align-items: center;
        right: 0;
        cursor: pointer;
        background: #ddd;
        /* border: 1px solid #ddd; */
        border-left: unset;
        position: absolute;
    }

    .item-post {
        display: inline-block;
        width: 100%;
        height: 183px;
        border: 1px solid #ddd;
        border-radius: 4px;
        padding: 10px;
        margin-bottom: 20px;
    }

    .post-right {
        display: inline-block;
        float: left;
        width: calc(100% - 250px);
        padding: 5px 0px 0px 18px;
        height: 100%;
        overflow: hidden;
    }

    .post-left {
        width: 250px;
        height: 100%;
        float: left;
    }

        .post-left img {
            height: 100%;
            width: 100%;
        }

    .breadcum ul li a {
        font-size: 15px;
    }
</style>
<section class="ftco-section ftco-degree-bg custom-height" style="    margin: 30px 0px;">

    <div>
        <div>
            <div class="container">
                <div class="breadcum" style="    margin-left: 25px;">

                    <ul>
                        <li><a title="Shop hoa tươi 360 | Dịch vụ điện hoa uy tín hoatuoi360.vn" href="/">Trang chủ</a></li>
                        /
                        <li><a href="javascript:void(0)">Danh sách bài viết</a></li>
                    </ul>
                    <div class="clear"></div>
                </div>
                <div class="row">
                    <div class="col-lg-9">
                        <div class="row box-details" style="margin-top: 20px;">
                            @if (Model.Results != null && Model.Results.Count > 0)
                            {
                                @foreach (var item in Model.Results)
                                {
                                    <div class="col-md-12">
                                        <div class="blog-entry align-self-stretch">
                                            <div class="text d-block pl-md-4">
                                                <a href="/chi-tiet-bai-viet/@item.id" class="item-post">
                                                    <div class="post-left">
                                                        <img src="/product-post/@item.avatar" />
                                                    </div>

                                                    <div class="post-right">
                                                        @* <div class="meta mb-3" style="margin-bottom: 5px !important">
                                                <div><span>@item.created_at.Value.ToString("dd/MM/yyyy")</span></div>
                                                <div><span>@item.author_name</span></div>
                                                </div>*@
                                                        <h3 class="heading">
                                                            @item.title
                                                        </h3>
                                                        <p class="short-content">@item.short_content</p>
                                                        @*  <p>
                                                <a href="/chi-tiet-bai-viet/@item.id" class="btn btn-primary py-2 px-3 view-more">Xem thêm</a>
                                                </p>*@
                                                    </div>

                                                </a>
                                            </div>
                                        </div>
                                    </div>
                                }
                            }
                        </div>
                    </div>
                    <div class="col-lg-3">
                        <div class="sidebar-box" style="margin-bottom:0px;padding-bottom:0px;margin-top: 20px;">

                            <div class="form-group" style="position: relative;">
                                <span class="fa fa-search btn-search-post"></span>
                                <input type="text" placeholder="Nhập từ khoá để tìm kiếm" class="form-control text-search-post">
                            </div>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
@*<section class="ftco-section ftco-degree-bg">
    <div>
        <div>
            <div class="container">
                <div class="row">
                    <div class="col-lg-8">
                        <div class="row box-details">
                            @if (Model.Results != null && Model.Results.Count > 0)
                            {
                                @foreach (var item in Model.Results)
                                {
                                    <div class="col-md-12 d-flex">
                                    <div class="blog-entry align-self-stretch d-md-flex">
                                        <img src="/product-post/@item.avatar" alt="" class="img-style block-20">
                                        <div class="text d-block pl-md-4">
                                            <div class="meta mb-3" style="margin-bottom: 5px !important">
                                                <div data-v-fabbb9d8=""><span data-v-fabbb9d8="">@item.created_at.Value.ToString("dd/MM/yyyy")</span></div>
                                                <div data-v-fabbb9d8=""><span data-v-fabbb9d8="">@item.author_name</span></div>
                                            </div>
                                            <h3 class="heading">
                                                @item.title
                                            </h3>
                                            <p class="short-content">@item.short_content</p>
                                            <p data-v-fabbb9d8=""><a href="/chi-tiet-bai-viet/@item.id" class="btn btn-primary py-2 px-3 view-more">Xem thêm</a></p>
                                        </div>
                                    </div>
                                </div>
                                }
                                
                            }

                           
                        </div>
                      
                    </div>
                    
                    <div class="col-lg-4 sidebar">
                        <div class="sidebar-box" style="margin-bottom:0px;padding-bottom:0px;">
                           
                                <div class="form-group" style="position: relative;">
                                    <span class="icon ion-ios-search btn-search-post"></span> 
                                    <input type="text" placeholder="Nhập từ khoá để tìm kiếm" class="form-control text-search-post"></div>
                           
                        </div>
                     @*  @await Component.InvokeAsync("ProductCountForCategoryIdPostComponent")
                       @await Component.InvokeAsync("RecentPostsComponent")
                    
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>*@

                                        
@section Scripts{
        <script type="text/javascript">
    $(document).ready(function () {
         $(".btn-search-post").click(function(){
                var textSearch = $(".text-search-post").val();
                if(textSearch != ""){
                    PostSearch(textSearch);
                }
                
               // Search(textSearch);
            })
            $(".text-search-post").keypress(function (e) {
              if (e.which == 13) {
                    var textSearch = $(".text-search-post").val();
                    if (textSearch != "") {
                        PostSearch(textSearch);
                    }
                return false;    //<---- Add this line
              }
            });
            


    })
     function PostSearch(textSearch) {
            $.ajax({
                url: '/Post/PostSearch',
                type: 'GET',
                dataType: "html",
                data: {
                    textSearch: textSearch
                },
                beforeSend: function () {
                    // Loading('show');
                },
                complete: function () {
                    // Loading('hiden');
                },
                success: function (response) {
                    $(".box-details").html(response);
                    //$("#ReviewModal").modal("show");

                }
            })

        }


</script>
}